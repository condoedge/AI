# Installing & Verifying the Stack

Use this walkthrough to install the package, wire up infrastructure, and confirm every integration (Neo4j, Qdrant, OpenAI/Anthropic, and LaRecipe-powered docs) is healthy.

> **You should have already read:** [Requirements & Compatibility](/docs/{{version}}/foundations/requirements)

---

## 1. Install the Package

```bash
composer require condoedge/ai
```

Laravel 10+ auto-discovers the service provider. For earlier versions, add it to `config/app.php`:

```php
'providers' => [
    // ...
    Condoedge\Ai\AiServiceProvider::class,
],
```

---

## 2. Publish Configuration

```bash
# Publish main configuration
php artisan vendor:publish --tag=ai-config

# Optional: Publish documentation assets (for LaRecipe)
php artisan vendor:publish --tag=ai-docs
```

**Files created:**
- `config/ai.php` - Main AI system configuration
- `resources/docs/1.0/` - LaRecipe documentation (optional)

**Note:** `config/entities.php` is generated by the `ai:discover` command (see step 6).

---

## 3. Configure Your Environment

Create or update `.env` with the minimum viable settings:

```env
# Neo4j Graph Database
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=change-me
NEO4J_DATABASE=neo4j

# Qdrant Vector Database
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_API_KEY=                 # Only needed for Qdrant Cloud

# LLM Provider
AI_LLM_PROVIDER=openai          # or anthropic
OPENAI_API_KEY=sk-your-key      # Required for OpenAI
ANTHROPIC_API_KEY=              # Required for Anthropic

# Embedding Provider
AI_EMBEDDING_PROVIDER=openai
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Auto-Discovery (DISABLED by default - use ai:discover command)
AI_AUTO_DISCOVERY_RUNTIME=false

# Auto-Sync (ENABLED by default)
AI_AUTO_SYNC_ENABLED=true
AI_AUTO_SYNC_QUEUE=false        # Set true for async processing

# Docs Portal (Optional)
AI_DOCS_ENABLED=true
AI_DOCS_PREFIX=ai-docs
```

**Important Settings:**
- `AI_AUTO_DISCOVERY_RUNTIME=false` - Runtime discovery is disabled for performance
- `AI_AUTO_SYNC_ENABLED=true` - Entities automatically sync to Neo4j + Qdrant on model events

Additional configuration options live in `config/ai.php`. See: [Configuration Reference](/docs/{{version}}/foundations/configuration)

---

## 4. Provision Neo4j & Qdrant (Docker Friendly)

```yaml
# docker-compose.ai.yml
version: '3.8'
services:
  neo4j:
    image: neo4j:5.15
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=neo4j/change-me
    volumes:
      - neo4j_data:/data

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  neo4j_data:
  qdrant_data:
```

Spin them up:

```bash
docker compose -f docker-compose.ai.yml up -d
```

- Browse Neo4j: <http://localhost:7474>
- Browse Qdrant: <http://localhost:6333/dashboard>

---

## 5. Smoke Test Every Integration

Create `scripts/test-ai-setup.php`:

```php
<?php

use Condoedge\Ai\VectorStore\QdrantStore;
use Condoedge\Ai\GraphStore\Neo4jStore;
use Condoedge\Ai\Facades\AI;

$neo4j = new Neo4jStore(config('ai.graph.neo4j'));
$schema = $neo4j->getSchema();
echo "Neo4j OK: labels=" . count($schema['labels']) . PHP_EOL;

$qdrant = new QdrantStore(config('ai.vector.qdrant'));
$collections = $qdrant->listCollections();
echo "Qdrant OK: collections=" . count($collections) . PHP_EOL;

if (config('ai.llm.default') === 'openai' && config('ai.llm.openai.api_key')) {
    $reply = AI::chat("Say 'Hello from AI Package'");
    echo "LLM OK: {$reply}" . PHP_EOL;
}

echo "Setup complete" . PHP_EOL;
```

Run inside `php artisan tinker` or directly via `php scripts/test-ai-setup.php`.

---

## 6. Enable In-App Docs (Optional But Recommended)

- Ensure `AI_DOCS_ENABLED=true`.
- Hit `http://your-app.test/ai-docs`.
- LaRecipe will render everything inside `resources/docs/1.0`. The three main groups are Foundations, Usage, and Internals.

Protect the docs behind auth by extending the middleware array in `config/ai.php`:

```php
'documentation' => [
    'route_prefix' => env('AI_DOCS_PREFIX', 'ai-docs'),
    'middleware' => ['web', 'auth'],
    // ...
],
```

---

## 7. Create Your First Nodeable Entity

Make a model Nodeable by implementing the interface and using the trait:

```php
use Illuminate\Database\Eloquent\Model;
use Condoedge\Ai\Domain\Contracts\Nodeable;
use Condoedge\Ai\Domain\Traits\HasNodeableConfig;

class Customer extends Model implements Nodeable
{
    use HasNodeableConfig;

    protected $fillable = ['name', 'email', 'company', 'status'];
}
```

**That's it!** The `HasNodeableConfig` trait enables auto-sync and discovery.

---

## 8. Discover Entity Configurations

Generate entity configurations automatically:

```bash
php artisan ai:discover
```

**What it does:**
- Analyzes all Nodeable models in your application
- Generates `config/entities.php` with graph and vector configurations
- Detects relationships, properties, and sensible embed fields
- Creates aliases and descriptions for RAG

**Output:**
```
🔍 Entity Auto-Discovery

Scanning for Nodeable models...

Found 3 model(s):
  - App\Models\Customer
  - App\Models\Order
  - App\Models\Product

✓ Configuration written to config/entities.php

Review and customize the configuration as needed.
```

**Review the generated config:**
```php
// config/entities.php
return [
    'App\Models\Customer' => [
        'graph' => [
            'label' => 'Customer',
            'properties' => ['id', 'name', 'email', 'company', 'status'],
            'relationships' => [
                ['type' => 'PLACED_ORDER', 'target_label' => 'Order']
            ]
        ],
        'vector' => [
            'collection' => 'customers',
            'embed_fields' => ['name', 'email', 'company'],
            'metadata' => ['id', 'name', 'status']
        ]
    ]
];
```

See: [Configuration Reference](/docs/{{version}}/foundations/configuration#entity-configuration-configentitiesphp)

---

## 9. Bulk Ingest Existing Data (If Any)

If you have existing data, ingest it into Neo4j + Qdrant:

```bash
php artisan ai:ingest
```

**What it does:**
- Finds all Nodeable entities in your database
- Processes them in batches (default: 100)
- Creates Qdrant collections automatically
- Shows progress bar with success/failure counts

**Output:**
```
🚀 Bulk Entity Ingestion

Found 3 Nodeable model(s)

Processing: App\Models\Customer
  Found 150 entities
  [████████████████] 150/150 (100%) Ingested: 150, Failed: 0
  ✓ Ingested: 150

✓ Successfully ingested: 150
✗ Failed: 0

Ingestion complete!
```

**Options:**
```bash
# Ingest specific model
php artisan ai:ingest --model="App\Models\Customer"

# Custom batch size
php artisan ai:ingest --chunk=500

# Preview without ingesting
php artisan ai:ingest --dry-run
```

**Note:** This is a **one-time operation** for existing data. New entities automatically sync via auto-sync!

See: [Data Ingestion Guide](/docs/{{version}}/usage/data-ingestion#bulk-ingestion-command)

---

## 10. Test Auto-Sync

Create a new entity and verify it's automatically ingested:

```php
use App\Models\Customer;
use Condoedge\Ai\GraphStore\Neo4jStore;

// Create a customer (auto-synced!)
$customer = Customer::create([
    'name' => 'Jane Doe',
    'email' => 'jane@example.com',
    'company' => 'Acme Corp',
    'status' => 'active',
]);

// Verify it's in Neo4j
$neo4j = app(Neo4jStore::class);
$results = $neo4j->query("MATCH (c:Customer {id: {$customer->id}}) RETURN c");

if (!empty($results)) {
    echo "✓ Customer found in Neo4j!\n";
} else {
    echo "✗ Customer not found - check logs\n";
}
```

**Expected behavior:**
- ✅ Customer created in database
- ✅ Node created in Neo4j automatically
- ✅ Vector embedded and stored in Qdrant automatically
- ✅ All relationships created automatically

If you see errors, check:
1. Auto-sync is enabled: `AI_AUTO_SYNC_ENABLED=true`
2. Neo4j and Qdrant are running
3. Laravel logs for error details

See: [Troubleshooting](/docs/{{version}}/foundations/troubleshooting)

---

## 11. Next Steps

**Get started quickly:**
- **[Quick Start Guide](/docs/{{version}}/usage/quick-start)** - 5-minute tutorial with natural language queries

**Learn the system:**
- **[Configuration Reference](/docs/{{version}}/foundations/configuration)** - All settings explained
- **[Data Ingestion Guide](/docs/{{version}}/usage/data-ingestion)** - Auto-sync, bulk ingestion, and manual operations
- **[Simple Usage (AI Facade)](/docs/{{version}}/usage/simple-usage)** - All AI wrapper methods

**Share documentation:**
- Visit `http://your-app.test/ai-docs` to access the in-app documentation
- Share the docs route with teammates for self-service onboarding
